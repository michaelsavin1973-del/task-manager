apiVersion: v1
kind: Service
metadata:
  name: task-manager-frontend
spec:
  type: NodePort
  selector:
    app: task-manager-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
