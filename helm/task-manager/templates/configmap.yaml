apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "task-manager.fullname" . }}-backend-config
  namespace: {{ .Release.Namespace }}

data:
  DB_HOST: {{ include "task-manager.fullname" . }}-postgres
  DB_NAME: {{ .Values.postgres.database }}
  DB_USER: {{ .Values.postgres.user }}
